#ifndef MERGESORT
#define MERGESORT
#include <iostream>
#include <string>
#include <vector>
#include "util.h"

void merge_sort(std::vector<int>&, int, int);
void sorted_merge(std::vector<int>&, int, int, int);

#endif