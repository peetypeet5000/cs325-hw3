#ifndef KNAP
#define KNAP

#include "util.h"

int recursive_knapsack(std::vector<int>, std::vector<int>, int, int);
int dp_knapsack(std::vector<int>, std::vector<int>, int, int);
int maximum(int, int);

#endif
